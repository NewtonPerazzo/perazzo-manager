import { CustomersTemplate } from "@/components/templates/dashboard/customers-template";

export default function CustomersPage() {
  return <CustomersTemplate initialData={[]} initialTotal={0} />;
}
